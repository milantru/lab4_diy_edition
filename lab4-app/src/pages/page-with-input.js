import TextInputWithResetButton from './../components/TextInputWithResetButton';

export default function PageWithInput() {
    return (
        <div>
            <h1>Page with input</h1>

            <p>Try to type something:</p>
            
            <TextInputWithResetButton />
        </div>
    );  
}
